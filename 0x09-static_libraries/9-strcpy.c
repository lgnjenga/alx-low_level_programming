#include "main.h"

/**
 * _strcpy - copy string using pointers
 *
 * @dest: para1
 * @src: para2
 *
 * Return: dest array string
 */
char *_strcpy(char *dest, char *src)
{
	/* function code comment */
	if (src)
		return (dest);
	else
		return (NULL);
}
