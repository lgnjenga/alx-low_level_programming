#include "main.h"

/**
 * main - Entry point, find change in cents
 *
 * @argc: argument count
 * @argv: argument vector
 *
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
	return (0);
}
